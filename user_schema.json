{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "birthday": {
      "type": "string",
      "format": "date-time",
      "description": "The user's birth date and time in timestamp format."
    },
    "created_time": {
      "type": "string",
      "format": "date-time",
      "description": "The time when the user account was created in timestamp format."
    },
    "display_name": {
      "type": "string",
      "description": "The display name of the user."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "The email address of the user."
    },
    "photo_url": {
      "type": "string",
      "format": "uri",
      "description": "The URL to the user's profile picture."
    },
    "uid": {
      "type": "string",
      "description": "A unique identifier for the user."
    }
  },
  "required": ["uid", "created_time", "display_name", "email"]
}
